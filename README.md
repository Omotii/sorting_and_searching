# sorting_and_searching algorithm

This algorithm aims at applying the insertion sort technique to sort arr[i] into place.

A nested loop is used to loop into the sub array and sort out arr[i] everytime it smaller than it preceding element.
